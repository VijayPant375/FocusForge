# Production API URL is injected by Render as an environment variable at build time.
# Do NOT hardcode a value here — set VITE_API_URL in the Render dashboard instead.
# Local development uses the fallback in src/api.js: http://localhost:5000/api
