# The port number on which the User Service listens
PORT=5001

# Connection string to the MongoDB database for user data (local or Atlas URI)
MONGODB_URI=mongodb://localhost:27017/habit-tracker-users

# The secret key used to sign and verify JSON Web Tokens (JWT) for authentication
JWT_SECRET=your-secure-secret-key-goes-here
